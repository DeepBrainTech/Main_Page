/**
 * Support inbox for Contact Us (mailto). Set NEXT_PUBLIC_SUPPORT_EMAIL in the deployment env to override.
 */
export const SUPPORT_CONTACT_EMAIL = process.env.NEXT_PUBLIC_SUPPORT_EMAIL ?? "test@dbtest.com";
