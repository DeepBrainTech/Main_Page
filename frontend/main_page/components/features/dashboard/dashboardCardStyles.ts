/** Shared glass card shell for dashboard sections (matches home.md: white/60 + outline/shadow) */
export const dashboardCardClass =
  "rounded-3xl bg-white/60 shadow-[0px_4px_6px_-4px_rgba(0,0,0,0.10)] shadow-lg outline outline-1 outline-offset-[-1px] outline-white/60";

/** Padding + header row/title for paired dashboard section cards (e.g. check-in + tasks) */
export const dashboardSectionPadding = "p-[clamp(0.9rem,1.5vw,1.5rem)]";
export const dashboardSectionHeaderRowClass =
  "mb-[clamp(0.6rem,1.2vw,1rem)] flex min-h-[2.25rem] shrink-0 items-center justify-between gap-3";
export const dashboardSectionTitleClass =
  "font-['Titan_One'] text-[clamp(1.1rem,2vw,1.5rem)] font-normal leading-tight text-[#045E96]";
