# 忘记密码功能说明

## ✅ 已完成的功能

成功为登录页面添加了完整的"忘记密码"功能！

## 🎯 功能特性

### 1. 登录页面增强
- ✅ 在密码字段旁边添加了"忘记密码？"链接
- ✅ 点击后跳转到忘记密码页面

### 2. 忘记密码流程
**第一步：输入邮箱**
- 用户输入注册时使用的邮箱
- 点击"发送验证码"
- 系统验证邮箱是否已注册
- 发送6位验证码到邮箱（中英文邮件）

**第二步：重置密码**
- 输入收到的验证码
- 输入新密码
- 确认新密码
- 提交后密码更新成功
- 自动跳转到登录页面

### 3. 安全特性
- ✅ 验证码5分钟有效期
- ✅ 60秒倒计时防止频繁发送
- ✅ 验证码一次性使用
- ✅ 密码最少6位
- ✅ 检查邮箱是否已注册

## 📁 修改的文件

### 前端

#### 新增文件：
1. **`frontend/main_page/app/[locale]/forgot-password/page.tsx`**
   - 忘记密码页面
   - 两步式流程（发送验证码 → 重置密码）
   - 倒计时功能
   - 表单验证

#### 修改文件：
1. **`frontend/main_page/app/[locale]/login/page.tsx`**
   - 在密码字段旁添加"忘记密码？"链接

2. **`frontend/main_page/language/zh.json`**
   - 添加忘记密码相关中文翻译
   - 添加登录页面的"忘记密码？"文本

3. **`frontend/main_page/language/en.json`**
   - 添加忘记密码相关英文翻译
   - 添加登录页面的"Forgot password?"文本

### 后端

#### 修改文件：
1. **`backend/api/schemas.py`**
   - 新增 `ResetPassword` 模型

2. **`backend/api/routes/auth.py`**
   - 新增 `/api/auth/send-reset-password-code` 端点（发送重置密码验证码）
   - 新增 `/api/auth/reset-password` 端点（重置密码）

## 🎨 界面展示

### 登录页面
```
┌─────────────────────────────────┐
│      DeepBrain Tech             │
│      登录您的账户                │
├─────────────────────────────────┤
│ 用户名                          │
│ [输入框]                         │
│                                 │
│ 密码          忘记密码？← 新增   │
│ [输入框]                         │
│                                 │
│        [登录]                   │
└─────────────────────────────────┘
```

### 忘记密码页面 - 第一步
```
┌─────────────────────────────────┐
│      忘记密码                    │
│      重置您的账户密码             │
├─────────────────────────────────┤
│ 邮箱                            │
│ [输入框]                         │
│                                 │
│   [发送验证码]                   │
│                                 │
│   返回登录                       │
└─────────────────────────────────┘
```

### 忘记密码页面 - 第二步
```
┌─────────────────────────────────┐
│      忘记密码                    │
│      重置您的账户密码             │
├─────────────────────────────────┤
│ 邮箱                            │
│ [已锁定的邮箱显示]               │
│                                 │
│ 验证码                          │
│ [输入框] [重新发送(60s)]         │
│                                 │
│ 新密码                          │
│ [输入框]                         │
│                                 │
│ 确认密码                         │
│ [输入框]                         │
│                                 │
│   [重置密码]                     │
│                                 │
│   返回登录                       │
└─────────────────────────────────┘
```

## 🔄 完整流程

```
用户点击"忘记密码？"
    ↓
进入忘记密码页面（第一步）
    ↓
输入邮箱地址
    ↓
点击"发送验证码"
    ↓
后端验证邮箱是否存在
    ↓
发送验证码到邮箱（中/英文）
    ↓
进入第二步
    ↓
输入验证码和新密码
    ↓
点击"重置密码"
    ↓
后端验证验证码
    ↓
更新密码
    ↓
显示成功消息
    ↓
2秒后自动跳转到登录页
```

## 📡 API 端点

### 1. 发送重置密码验证码

**端点：** `POST /api/auth/send-reset-password-code`

**请求：**
```json
{
  "email": "user@example.com",
  "language": "zh"  // 或 "en"
}
```

**成功响应：**
```json
{
  "success": true,
  "message": "VERIFICATION_CODE_SENT",
  "data": {
    "email": "user@example.com"
  }
}
```

**错误响应：**
```json
{
  "detail": "EMAIL_NOT_FOUND"  // 邮箱未注册
}
```

### 2. 重置密码

**端点：** `POST /api/auth/reset-password`

**请求：**
```json
{
  "email": "user@example.com",
  "verification_code": "123456",
  "new_password": "newpassword123"
}
```

**成功响应：**
```json
{
  "success": true,
  "message": "PASSWORD_RESET_SUCCESS",
  "data": {
    "email": "user@example.com"
  }
}
```

**错误响应：**
```json
{
  "detail": "VERIFICATION_CODE_INVALID"  // 验证码错误或已过期
}
```

## 🧪 测试步骤

### 测试中文流程

1. **访问登录页面**
   ```
   http://localhost:3000/zh/login
   ```

2. **点击"忘记密码？"**
   - 跳转到 `http://localhost:3000/zh/forgot-password`

3. **输入邮箱并发送验证码**
   - 输入注册时使用的邮箱
   - 点击"发送验证码"
   - 检查邮箱收到中文验证码邮件

4. **输入验证码和新密码**
   - 输入6位验证码
   - 输入新密码（至少6位）
   - 确认新密码
   - 点击"重置密码"

5. **验证重置成功**
   - 看到"密码重置成功"提示
   - 2秒后自动跳转到登录页
   - 使用新密码登录

### 测试英文流程

访问 `http://localhost:3000/en/login`，流程同上，界面为英文。

## ⚠️ 错误处理

系统会处理以下错误情况：

1. **邮箱未注册**
   - 提示："该邮箱未注册"

2. **验证码错误**
   - 提示："验证码错误或已过期"

3. **密码不匹配**
   - 提示："两次输入的密码不一致"

4. **密码太短**
   - 提示："密码长度至少为6位"

5. **验证码发送失败**
   - 提示："邮件发送失败"

## 🔒 安全特性

1. ✅ **验证码过期机制**
   - 5分钟后自动失效

2. ✅ **防止暴力破解**
   - 60秒倒计时限制发送频率

3. ✅ **验证码一次性使用**
   - 验证成功后立即删除

4. ✅ **邮箱验证**
   - 只有已注册的邮箱才能重置密码

5. ✅ **密码加密存储**
   - 使用 bcrypt 哈希存储密码

## 📝 使用的技术

### 前端
- Next.js 14
- TypeScript
- React Hooks (useState, useEffect)
- next-intl (国际化)
- Tailwind CSS

### 后端
- FastAPI
- Pydantic (数据验证)
- SQLAlchemy (数据库操作)
- 邮件服务（aiosmtplib）
- 验证码服务（Redis/内存）

## ✨ 用户体验亮点

1. **两步式流程**
   - 清晰的步骤引导
   - 不会让用户感到困惑

2. **实时反馈**
   - 成功/错误提示清晰
   - 倒计时显示
   - 按钮状态变化

3. **自动跳转**
   - 重置成功后自动跳转登录页
   - 无需手动导航

4. **多语言支持**
   - 中英文界面
   - 中英文邮件

5. **响应式设计**
   - 支持深色模式
   - 适配移动设备

## 🎯 功能演示

### 场景1：用户忘记密码

1. 用户打开登录页面，发现忘记密码
2. 点击密码字段旁边的"忘记密码？"
3. 进入忘记密码页面
4. 输入邮箱：`user@example.com`
5. 点击"发送验证码"
6. 收到邮件，验证码为：`123456`
7. 页面自动进入第二步
8. 输入验证码：`123456`
9. 输入新密码：`newpassword123`
10. 确认密码：`newpassword123`
11. 点击"重置密码"
12. 看到成功提示
13. 2秒后自动跳转到登录页
14. 使用新密码成功登录 ✅

### 场景2：邮箱未注册

1. 用户输入未注册的邮箱
2. 点击"发送验证码"
3. 显示错误："该邮箱未注册"
4. 用户返回注册页面注册账号

### 场景3：验证码过期

1. 用户收到验证码后5分钟没有使用
2. 输入验证码并提交
3. 显示错误："验证码错误或已过期"
4. 点击"重新发送"获取新验证码

## 💡 与注册页面的协同

忘记密码功能复用了注册页面的以下组件：

1. **邮件服务** (`email_service.py`)
   - 同一个邮件模板
   - 同一个发送逻辑

2. **验证码服务** (`verification_service.py`)
   - 同一个验证码生成和验证逻辑
   - 同一个存储机制

3. **多语言支持**
   - 根据页面语言发送对应语言的邮件

## 🚀 下一步优化建议

1. **增强安全性**
   - 添加图形验证码
   - 记录重置密码操作日志
   - 限制每天重置次数

2. **改进用户体验**
   - 添加密码强度提示
   - 显示密码要求（大小写、数字等）
   - 添加进度指示器

3. **邮件优化**
   - 自定义邮件模板样式
   - 添加品牌Logo
   - 添加找回账号的帮助链接

4. **监控和分析**
   - 统计重置密码成功率
   - 监控异常重置行为
   - 发送告警通知

## 🎉 总结

忘记密码功能已完全实现！主要特点：

✅ **完整的流程** - 从发送验证码到重置密码  
✅ **安全可靠** - 验证码机制，防止暴力破解  
✅ **用户友好** - 清晰的步骤，良好的反馈  
✅ **多语言支持** - 中英文界面和邮件  
✅ **响应式设计** - 支持移动设备和深色模式  

用户现在可以通过邮箱验证码轻松重置密码了！

---

**实现日期：** 2026-02-12  
**文档版本：** v1.0.0
